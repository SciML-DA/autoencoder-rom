# convergence_new / bl

Boundary-layer convergence study rerun with the optimised JAX autoencoders.
Same 1798/600/600 split and latent sweep as `../../convergence/bl/`, which is
the file to diff this against. See `../README.md`.
