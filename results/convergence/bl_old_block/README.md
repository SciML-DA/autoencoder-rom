# convergence / bl_old_block

Same study as `../bl/` — `scripts/convergence_study.py` on the boundary layer —
but an **earlier, smaller** blocked split: 598/200/200 instead of 1798/600/600,
and no split-diagnostic columns in the CSV.

Superseded by `../bl/`. Kept for comparison only.
